version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: ./app/dockerfile
      cache_from:
        - study_task-app:latest
    container_name: study_task_app
    ports:
      - "8080:8080"
    environment:
      - api_version=VERSION
    volumes:
      - ./app:/app